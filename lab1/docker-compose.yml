services:
    postgres:
        image: postgres:latest
        ports:
          - "5432:5432"
        environment:
          POSTGRES_PASSWORD: postgres
        volumes:
          - ./data:/data
          - ./sql:/docker-entrypoint-initdb.d/